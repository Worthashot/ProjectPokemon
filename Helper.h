#pragma once
#include <sstream>
#include <algorithm>
#include <iterator>
#include <vector>
using namespace std;
class Helper
{
public:
	Helper();
	vector<string> split(string const&);
	~Helper();
};

